// The JWRPreferences test double lives in JWRPreferencesStub.m and reuses the
// production interface (JWRPreferences.h). Keep this placeholder out of the
// include path so no translation unit sees a second @interface declaration.
#import "JWRPreferences.h"
